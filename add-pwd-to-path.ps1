$env:path += ";." # I don't really know if this will persist between sessions, etc. In fact, I'm pretty confident it will not. So maybe I have to mess with some kind of .profile file for Windows? Ideally this would also work for cmd...
