[ "$#" -ne 1 ] && echo USAGE: "$0 command" && echo && echo "IMPLEMENTATION:" && cat "$0" && echo && exit #usage message for invalid number of arguments
for i in `whereis "$@"` ; do cd `dirname "$i"` ; done
explorer . #turns out I always want to do this. If you aren't on WSL and explorer isn't a command on your system, I'm sorry. #TODO: wait, why doesn't this work? is this a fluke bug right now? check later