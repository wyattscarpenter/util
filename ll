# ll alias in case you don't have it already (See https://pubs.opengroup.org/onlinepubs/9699919799/utilities/ls.html for description of flags.)
# This alias is borrowed (without major modification), from the .bashrc on Ubuntu (and maybe other bashrcs; who can say).
ls -alF "$@"
