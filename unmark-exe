#this command solves the extremely specific problem where you're using WSL and you need to make an exe also function as a command without an .exe filename extension
#at the moment, this script is untested
[ "$#" -eq 0 ] && echo USAGE: "$0 command" && echo && echo "IMPLEMENTATION:" && cat "$0" && echo && exit #usage message for invalid number of arguments
for i in `whereis "$@"` ; do cp "$i" "${i/.exe}" ; done
explorer . #turns out I always want to do this. If you aren't on WSL and explorer isn't a command on your system, I'm sorry.